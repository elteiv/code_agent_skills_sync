# Claude Code Skill Sync Hook

This workspace now uses a Python SessionStart hook script for syncing remote skill files into a project.

See `PYTHON-README.md` for setup, manifest format, environment variables, and the Claude Code hook example.